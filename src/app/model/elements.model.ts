export interface Elemento {
  id: string;
  category: string;
  name: string;
  price: number;
  description:string;
}

export interface newElement {
  category: string;
  name: string;
  price: number;
  description:string;
}
